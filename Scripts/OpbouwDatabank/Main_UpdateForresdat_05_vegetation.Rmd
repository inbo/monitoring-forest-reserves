---
title: "Update forresdat 5 - vegetatie"
author: "Anja Leyman"
date: "`r Sys.Date()`"
output: 
  html_document:
    keep_md: FALSE
    number_sections: yes
    fig_caption: yes
    code_folding: hide
    toc: TRUE
    toc_float:
      collapsed: yes
      smooth_scroll: yes
---

```{r Rm, eval = FALSE}
rm(list=ls())
```

```{r Setup, include = FALSE}
knitr::opts_chunk$set(
  echo = TRUE,
  eval = FALSE,  # FALSE: dan kan ik knitten zonder dat er iets gebeurt
  message = FALSE,
  warning = FALSE, 
  fig.width = 9,
  fig.align = TRUE)

library(here)

# libraries & invoergegevens & functions
source(here::here("scripts/Setup.R"))

```


```{r Setup2}
path_to_html_teamdrive <- paste0(path_to_dataverwerking_teamdrive, "25_Overkoepelend")
```

> juiste forrescalc kiezen:   
(1)  om weg te schrijven naar "main" branch van forresdat => met juiste release van het package: dan `install.packages("forrescalc")`  
OF
(2) om weg te schrijven naar "develop" branch van forresdat, dat kan met laatste correcties van forrescalc 
=> juiste branch selecteren in forrescalc en opnieuw builden 

> (10/3/25: branch 'bugfix' met corr van read_forresdat waarbij we de develop branch kunnen selecteren ipv main)

> (15/12/2025: branch "bugfix_AL2025" => wegschrijven naar branch develop van forresdat)

```{r forrescalc_to_write_to_forresdat_NOG_NIET, eval = FALSE}
# !!! eerst zeker 'install.packages("forrescalc")
# zodat forresdat-data goede release krijgt!
# zie mail 7/10/24 van Els L.
# soms een andere branch nodig als Els nog iets moet fixen, maar als ik naar forresdat 
# wil wegschrijven, moet ik officiële versie van forrescalc gebruiken

install.packages("forrescalc")

```


# Vraagstelling
DOEL van dit script is een update van forresdat gegevens naar 

- forresdat-folder als tsv (nadien nog te pushen naar github)
- BR_analysedb.accdb
- output-folders "_plot-level-data" en "_tree-level-data" als csv
- gdrive-folders "_plot-level-data" en "_tree-level-data" als csv

Kersselaerspleyn blijkt op meerdere vlakken uitzonderingen te bevatten.
Deze uitzonderingen worden na het inladen van de data gecorrigeerd. 


```{r results='hold', eval=TRUE}
path_to_forresdat_data
path_to_analysis_set_db
path_to_plotlevel_csv
path_to_treelevel_csv
path_to_output_gdrive
path_to_meetgegevens

```


# Load plotinfo

Plotinfo inladen: `plottype`, naam `forest_reserve` en info over survey's.
(plotinfo: enkel processed plots!)
Plotinfo inladen: `plottype`, naam `forest_reserve` en info over survey's.
(plotinfo op gdrive en forresdat: enkel processed plots!)

```{r }
# mbv forresdat
# plotinfo <- read_forresdat(file = "plotinfo", root = path_to_forresdat_data)
# names(plotinfo)
# ?read_forresdat

# van c-schijf
    # plotinfo_ <- read.csv2(paste0(path_to_plotlevel_csv, "plotinfo.csv")) %>%
    #   select(-1)

# van forresdat
plotinfo <- read_forresdat_table(
  tablename = "plotinfo"
  , git_ref_type = "branch", git_reference = "develop"
  , join_plotinfo = FALSE
  , plottype = "all")  # NIET ENKEL CP's!

# waldo::compare(plotinfo_, plotinfo)
```

# Load lookuplijsten

```{r load_lkp}
con <- odbcConnectAccess2007(path_to_fieldmap_db)

  qHerbSpecies240810<- sqlFetch(con, "qHerbSpecies240810", stringsAsFactors = FALSE)
  qCoverHerbs <- sqlFetch(con, "qCoverHerbs", stringsAsFactors = FALSE)
  qBrowsIndex <- sqlFetch(con, "qBrowsIndex", stringsAsFactors = FALSE)
  
odbcClose(con)

```


# Load vegetation - OK - 19/1/2023 - 18/3/2025 - 23/12/2025

```{r load_veg}
data_vegetation <-
  load_data_vegetation(
    database = path_to_fieldmap_db,
    processed = TRUE
  ) %>% 
  # nog onterecht data_processed = TRUE:
  filter(!(forest_reserve == "Bos Terrijst" & period == 3))

data_herblayer <-
  load_data_herblayer(
    database = path_to_fieldmap_db,
    processed = TRUE
  ) %>% 
  # nog onterecht data_processed = TRUE:
  filter(!(forest_reserve == "Bos Terrijst" & period == 3))
```

# Calculate

```{r}
vegetation <- calculate_vegetation(data_vegetation, data_herblayer)
```


```{r CreateSeparateDataframesVeg}
for (tablename in names(vegetation)) {
     assign(tablename, vegetation[[tablename]])
} 

```


# Controle plot-level-data

```{r}
data_vegetation %>% filter(plot_id == 1176 & period == 1)
data_herblayer %>% filter(plot_id == 1176)
data_herblayer %>% filter(plot_id == 1176 & period == 1)

veg_by_plot %>% filter(plot_id == 1176 & period == 1)

```


# Check NA's

```{r results='hide'}
names(veg_by_plot)
summary(veg_by_plot)
# veel covers niet ingevuld ...
```

```{r}
col <- c("number_of_species", "cumm_herb_coverage_class_average_perc")

veg_by_plot %>% filter_at(col, any_vars(is.na(.))) %>% 
  filter(number_of_species != 0) %>% nrow() == 0
# 42 met cumm_herb_coverage_class_average_perc = NA, allemaal aantal soorten = 0
veg_by_plot %>% filter_at(col, any_vars(is.na(.))) %>% nrow()
veg_by_plot %>% filter(number_of_species == 0) %>% nrow()
```

In `data_herblayer` wordt een record met species = NA toegevoegd voor elke plot waar een opname gebeurd
is. Dus elke plot met een veg-opname is opgenomen in `data_herblayer` = `herblayer_by_plot`.

```{r check_species_NA}
n_distinct(data_herblayer$plot_id, data_herblayer$period) == 
  n_distinct(data_vegetation$plot_id, data_vegetation$period)
# Doordat in herblayer een record met species = NA wordt toegevoegd voor elke plot waar een opname gebeurd
# is, zijn er hier rare NA's

data_herblayer %>% filter(is.na(species)) %>% nrow() ==
  veg_by_plot %>% filter(number_of_species == 0) %>% nrow()

```

Aanpassing in forrescalc gedaan (branch "bugfix_AL2") functie `load_data_herblayer`:
toevoegen van "AND Herb.species IS NOT NULL"

```
 IIf(Herb.browse_index_id IS NULL AND pd.GameImpactVegObserved = 10
    AND Herb.species IS NOT NULL,
             100, Herb.browse_index_id) AS browse_index_id,
```

(ervoor: plot 410000, period 3, species = NA én browse_index_id = 100 = "geen vraatschade")

```{r browse_index_species_NA}
names(veg_by_core_area_species)
names(data_herblayer)

data_herblayer %>% filter(plot_id == 41000 & is.na(species))
data_herblayer %>% filter(plot_id == 41000 & subplot_id == 22)
# period 3, browse-index = 100 "Geen vraatschade", maar er is ook geen soort genoteerd!!
# dat zou eerder NA moeten zijn
# bij period 1 & 2 geen opname gebeurd => browse-index = NA

# als dit doorgerekend wordt naar veg_by_core_area_species, ook niet OK
veg_by_core_area_species %>% filter(plot_id == 41000 & is.na(species))
# number_of_subplots_with_species = 1  (species = NA)
# number_of_subplots_browsed = 0
# HEEFT GEEN BETEKENIS!!!

# komt dit ook voor bij bv. cirkelplots? NEE
data_herblayer %>% filter(is.na(species) & !is.na(browse_index_id) & plottype == "CP") %>% 
  nrow() == 0
data_herblayer %>% left_join(plotinfo) %>% 
  filter(is.na(species) & game_impact_veg == TRUE & plottype == "CP") %>% 
  nrow() == 0
```


```{r subplots_without_species}
names(veg_by_core_area_species)

col <- c("number_of_subplots_with_species", "perc_of_subplots", "mean_coverage_class_average_perc")

veg_by_core_area_species %>% filter_at(col, any_vars(is.na(.))) %>% nrow()
t <- veg_by_core_area_species %>% filter_at(col, any_vars(is.na(.)))
# 2 records met mean_coverage_class_average_perc = NA wegens species = NA
t1 <- data_herblayer %>% filter(plottype == "CA" & is.na(species))
unique(t1$plot_id)
# [1] 11000 41000


```


- plot 11000 - periode 1: 750 subplots, maar slechts 710 mét kruidlaag
=> 40 plot zonder species => `mean_coverage_class_average_perc` = NA - OK
(`number_of_subplots_with_species` = 40)
!! jaartal = 2000 = OK, want er was een opname

- plot 41000: `species` = NA => `mean_coverage_class_average_perc` = NA - OK

<!-- # OUDERE ANALYSE, VOOR AANPASSING FORRESCALC:  -->
<!-- - plot 31000 en 43000: 40 subplots zonder species     -->

<!-- REDEN: in periode 1 werden deze 40 subplots niet opgemeten -->
<!-- maar dat is niet opgenomen in plotdetails (veg_survey = TRUE), want dat is info  -->
<!-- op plotniveau, niet de subplots     -->
<!-- (11000: ook 40 plots met species = NA, maar daar wel een jaartal => dat duidt onderscheid met waar -->
<!-- dan wél een opname gebeurd is) -->

<!-- OPLOSSING: als jaartal = NA, dan moeten die records verwijderd worden uit data_herblayer -->
<!-- => aangepast in branch "bugfix_AL2" (27/3/2025) -->
<!-- Bij berekening van `veg_by_core_area_species` worden deze dan ook niet meer meegenomen -->


```{r}
data_herblayer %>% filter(plottype == "CP" & is.na(species))
# plot 1176: OK, geen kruidlaag aanwezig 
```


# Vergelijking met recente verwerking JHB/Wijn

```{r eval = FALSE}
path_to_JHB <- "G:/Gedeelde drives/Team_Boseco_BR/PRJ_BR_Gegevensverwerking/20_Per_gebied/2025_Jansheideberg/03_output_R_plotlevel/"
veg_by_plot_JHB_gdrive <- read_xlsx(paste0(path_to_JHB, "veg_by_plot.xlsx"))

veg_by_plot_JHB <- veg_by_plot %>% 
  left_join(plotinfo %>% select(forest_reserve, plot_id, period)) %>% 
  filter(forest_reserve == "Jansheideberg") 
                           
# vgl
veg_by_plot_JHB_gdrive <- veg_by_plot_JHB_gdrive %>% 
  select(-1, -survey_number) 

nrow(veg_by_plot_JHB_gdrive);nrow(veg_by_plot_JHB)
names(veg_by_plot_JHB_gdrive) == names(veg_by_plot_JHB)
waldo::compare(veg_by_plot_JHB_gdrive, veg_by_plot_JHB)

# t <- veg_by_plot_JHB_gdrive %>% 
#   anti_join(veg_by_plot_JHB)
# 
# t <- veg_by_plot_JHB %>% 
#   anti_join(veg_by_plot_JHB_gdrive)
# 
# head(veg_by_plot %>% arrange(desc(plot_id), period)); 
# head(veg_by_plot_JHB %>% arrange(desc(plot_id), period))
```


```{r eval = FALSE}
path_to_wijn <- "G:/Gedeelde drives/Team_Boseco_BR/PRJ_BR_Gegevensverwerking/20_Per_gebied/2024_Wijnendale/03_output_R_plotlevel/"
veg_by_core_area_species_gdrive <- read_xlsx(paste0(path_to_wijn, "vegetation_by_core_area_species.xlsx"))

veg_by_core_area_species_wijn <- veg_by_core_area_species %>% 
  left_join(plotinfo %>% select(forest_reserve, plot_id, period)) %>% 
  filter(forest_reserve == "Wijnendalebos") 
                           
# vgl
veg_by_core_area_species_gdrive <- veg_by_core_area_species_gdrive %>% 
  select(-1, -survey_number) 

nrow(veg_by_core_area_species_gdrive);nrow(veg_by_core_area_species_wijn)
names(veg_by_core_area_species_gdrive) == names(veg_by_core_area_species_wijn)
waldo::compare(veg_by_core_area_species_gdrive, veg_by_core_area_species_wijn)

t <- veg_by_core_area_species_gdrive %>% 
  anti_join(veg_by_core_area_species_wijn)

t2 <- veg_by_core_area_species_wijn %>% 
  anti_join(veg_by_core_area_species_gdrive)

head(veg_by_core_area_species_wijn %>% arrange(desc(plot_id), period)); 
head(veg_by_core_area_species_gdrive %>% arrange(desc(plot_id), period))
# beter, nu rekening gehouden met feit dat browsing = 0 ipv NA
                             
```

```{r eval = FALSE}
path_to_JHB <- "G:/Gedeelde drives/Team_Boseco_BR/PRJ_BR_Gegevensverwerking/20_Per_gebied/2025_Jansheideberg/03_output_R_plotlevel/"
veg_by_core_area_species_gdrive <- read_xlsx(paste0(path_to_JHB, "veg_by_core_area_species.xlsx"))

veg_by_core_area_species_JHB <- veg_by_core_area_species %>% 
  left_join(plotinfo %>% select(forest_reserve, plot_id, period)) %>% 
  filter(forest_reserve == "Jansheideberg") 
                           
# vgl
veg_by_core_area_species_gdrive <- veg_by_core_area_species_gdrive %>% 
  select(-1, -survey_number) 

nrow(veg_by_core_area_species_gdrive);nrow(veg_by_core_area_species_JHB)
names(veg_by_core_area_species_gdrive) == names(veg_by_core_area_species_JHB)
waldo::compare(veg_by_core_area_species_gdrive, veg_by_core_area_species_JHB)

t <- veg_by_core_area_species_gdrive %>% 
  anti_join(veg_by_core_area_species_JHB)

t2 <- veg_by_core_area_species_JHB %>% 
  anti_join(veg_by_core_area_species_gdrive)

head(veg_by_core_area_species_JHB %>% arrange(desc(plot_id), period)); 
head(veg_by_core_area_species_gdrive %>% arrange(desc(plot_id), period))

                             
```


```{r eval = FALSE}
path_to_wijn <- "G:/Gedeelde drives/Team_Boseco_BR/PRJ_BR_Gegevensverwerking/20_Per_gebied/2024_Wijnendale/03_output_R_plotlevel/"
veg_by_plot_height_species_gdrive <- read_xlsx(paste0(path_to_wijn, "veg_by_plot_height_species.xlsx"))

veg_by_plot_height_species_wijn <- veg_by_plot_height_species %>% 
  left_join(plotinfo %>% select(forest_reserve, plot_id, period)) %>% 
  filter(forest_reserve == "Wijnendalebos") 
                           
# vgl
veg_by_plot_height_species_gdrive <- veg_by_plot_height_species_gdrive %>% 
  select(-1, -survey_number) 

nrow(veg_by_plot_height_species_gdrive);nrow(veg_by_plot_height_species_wijn)
names(veg_by_plot_height_species_gdrive) == names(veg_by_plot_height_species_wijn)
waldo::compare(veg_by_plot_height_species_gdrive, veg_by_plot_height_species_wijn)

t <- veg_by_plot_height_species_gdrive %>% 
  anti_join(veg_by_plot_height_species_wijn, by = c("plot_id", "period", "species", "height_class"))

t <- veg_by_plot_height_species_wijn %>% 
  anti_join(veg_by_plot_height_species_gdrive, by = c("plot_id", "period", "species", "height_class"))

head(veg_by_plot_height_species_wijn %>% arrange(desc(plot_id), period)); 
head(veg_by_plot_height_species_gdrive %>% arrange(desc(plot_id), period))
```

# Vergelijking met oude data

22/12/2025 vergeleken met data op git (maart 2025 (?))

```{r eval = FALSE, results='hide'}
# 
veg_by_plot_git <- read_forresdat_table(tablename = "veg_by_plot"
                                           , join_plotinfo = FALSE
                                           , plottype = "all"
                                           , git_ref_type = "branch"
                                           , git_reference = "develop")

# vgl
veg_by_plot_git <- veg_by_plot_git %>% 
  select(plot_id, year_main_survey, everything()) 
# %>% 
#   select(names(dendro_by_plot))    # vol_log_min40cm van naam veranderd!
nrow(veg_by_plot);nrow(veg_by_plot_git)
# [1] 6158
# [1] 6158
summary(veg_by_plot)
names(veg_by_plot) == names(veg_by_plot_git)
summary(veg_by_plot_git)
waldo::compare(veg_by_plot, veg_by_plot_git)

t <- veg_by_plot %>% 
  anti_join(veg_by_plot_git) %>% 
  left_join(plotinfo %>% select(forest_reserve, plot_id, period)) 

t2 <- veg_by_plot_git %>% 
  anti_join(veg_by_plot) %>% 
  left_join(plotinfo %>% select(forest_reserve, plot_id, period)) 

head(veg_by_plot %>% arrange(plot_id, period)); 
head(veg_by_plot_git %>% arrange(plot_id, period))


t %>% filter(plot_id == 101); t2 %>% filter(plot_id == 101)
tt <- rbind(t %>% filter(plot_id == 11000 & subplot_id == 155545), t2 %>% filter(plot_id == 11000 & subplot_id == 155545))
```

```{r eval = FALSE, results='hide'}
# 
veg_by_core_area_species_git <- read_forresdat_table(tablename = "veg_by_core_area_species"
                                           , join_plotinfo = FALSE
                                           , plottype = "all"
                                           , git_ref_type = "branch"
                                           , git_reference = "develop")

# vgl
veg_by_core_area_species_git <- veg_by_core_area_species_git %>% 
  select(plot_id, year, everything()) 
# %>% 
#   select(names(dendro_by_plot))    # vol_log_min40cm van naam veranderd!
nrow(veg_by_core_area_species);nrow(veg_by_core_area_species_git)
# summary(veg_by_core_area_species)
names(veg_by_core_area_species) == names(veg_by_core_area_species_git)
summary(veg_by_core_area_species_git)
waldo::compare(veg_by_core_area_species, veg_by_core_area_species_git)

t <- veg_by_core_area_species %>% 
  anti_join(veg_by_core_area_species_git, by = c("plot_id", "period"))
t <- veg_by_core_area_species %>% 
  anti_join(veg_by_core_area_species_git)
            
head(veg_by_core_area_species %>% arrange(desc(plot_id), period)); 
head(veg_by_core_area_species_git %>% arrange(desc(plot_id), period))

tt <- rbind(veg_by_core_area_species %>% filter(plot_id == 41000 & species == 760)
            , veg_by_core_area_species_git %>% filter(plot_id == 41000 & species == 760))
# afronding ...
```


# Export vegetation - OK - 23/12/2025

Ook naar git exporteren

```{r join_plotinfo_veg}
veg_plotinfo <- lapply(vegetation, function(x) left_join(x, plotinfo %>% select(plot_id, forest_reserve, plottype, period, survey_number), by = c("plot_id", "period")))
# lapply: same function over all elements of the list
```


```{r save_veg_git, eval = FALSE}
# geen plotinfo naar git
save_results_forresdat(
  results = vegetation,
  repo_path = path_to_git_forresdat,
  metadata_path = paste0(path_to_metadata, "_metadata_plotlevel_results_veg.xlsx"),
  push = FALSE,
  strict = TRUE,
  branch = "develop"
)
```


```{r save_veg_access_csv, eval = FALSE}
# volgende functie heb ik zelf geschreven, zit niet in het package!
save_results_csv (
    results = veg_plotinfo,
    output_dir = path_to_plotlevel_csv
    )

# afgerond voor export naar access
# de df's van CP mogen op 0 afgerond worden (= 1ste drie dataframes)
# de KV's op 2 cijfers na de komma

round_df <- function(df, digits) {
  df[] <- lapply(df, function(x) if(is.numeric(x)) round(x, digits) else x)
  return(df)
}

veg_round <- veg_plotinfo %>% 
  lapply(., function(x) round_df(x, 2))

# save_results_access (
#   results = veg_round,
#   database = path_to_analysis_set_db,
#   remove_tables = TRUE
#   )

# Functie "save_results_access" (< package) geeft problemen met dates => hierna manueel
veg_by_plot <- veg_by_plot %>% 
  mutate(date_vegetation = as.Date(gsub(' SAST','',date_vegetation))) 
  # gsub (SAST) is om tijdzone weg te halen, anders wordt een dag vroeger weergegeven


con <- odbcConnectAccess2007(path_to_analysis_set_db)

# veg_by_plot
dbtables <- sqlTables(con)
if ("veg_by_plot" %in% dbtables$TABLE_NAME) {
        sqlDrop(con, "veg_by_plot")
      }
sqlSave(con, dat = veg_by_plot, tablename = "veg_by_plot", varTypes = c(date_vegetation="datetime"))


# veg_by_core_area_species
dbtables <- sqlTables(con)
if ("veg_by_core_area_species" %in% dbtables$TABLE_NAME) {
        sqlDrop(con, "veg_by_core_area_species")
      }
sqlSave(con, dat = veg_by_core_area_species, tablename = "veg_by_core_area_species")

odbcClose(con)
```


Direct naar gdrive exporteren (csv): 
naar **/PRJ_BOSECO_ALGEMEEN/PRJ_BR_AanvraagGegevens/!METADATA-ALL_PLOTS/_plot-level-data/**

```{r save_veg_gdrive, eval = FALSE}
save_results_csv(
    results = veg_plotinfo,
    output_dir = path_to_plotlevel_gdrive
    )

```


# Export herblayer - OK - 23/12/2025

Ook herblayer naar forresdat wegschrijven

```{r select_variables}
names(data_herblayer)

con <- odbcConnectAccess2007(path_to_fieldmap_db)
qBrowsIndex <- sqlFetch(con, "qBrowsIndex")
# qCoverHerbs <- sqlFetch(con, "qCoverHerbs")
odbcClose(con)

herblayer <- data_herblayer %>%
  select(plot_id, subplot_id, period, 
         date_vegetation, year, 
         species, coverage_class_average_perc, 
         browse_index_id) %>%
  left_join(qBrowsIndex %>% select(ID, Value2), by = c("browse_index_id" = "ID")) %>% 
  rename(browse_index = Value2) %>% 
  # mutate(date_vegetation = as.Date(date_vegetation)) 
  # !! r as.Date returns previous day 
  mutate(date_vegetation = as.Date(gsub(' SAST','',date_vegetation))) 
  # Which will remove the timezone from part of the string and then convert to date.

# save_results_xxxx: gemaakt om list weg te schrijven
# hier maar één dataframe
herblayer_list <- list(herblayer_by_plot = herblayer)
herblayer_list_git <- list(herblayer_by_plot = herblayer %>% select(-browse_index))
# summary(herblayer)

```

```{r join_plotinfo_herb}
herb_plotinfo <- lapply(herblayer_list, function(x) left_join(x, plotinfo %>% select(plot_id, forest_reserve, plottype, period, survey_number), by = c("plot_id", "period")))
# lapply: same function over all elements of the list

```

```{r save_herb_git, eval = FALSE}
# geen plotinfo naar git
save_results_forresdat(
  results = herblayer_list_git,
  repo_path = path_to_git_forresdat,
  metadata_path = paste0(path_to_metadata, "_metadata_plotlevel_results_veg.xlsx"),
  push = FALSE,
  strict = TRUE,
  branch = "develop"
)

# check: echt niet veranderd??
# check <- read_vc(file = "herblayer_by_plot", root = path_to_forresdat_data)
# check %>% filter(plot_id == 41000 & period == 3 & browse_index_id == 100)
# herblayer %>% filter(plot_id == 41000 & period == 3 & browse_index_id == 100)
```

```{r save_herb_access_csv, eval = FALSE}
# volgende functie heb ik zelf geschreven, zit niet in het package!
save_results_csv (
    results = herb_plotinfo,
    output_dir = path_to_plotlevel_csv
    )

# afgerond voor export naar access
herb_round <- herb_plotinfo %>% 
  lapply(., function(x) round_df(x, 2))

# save to access nog steeds niet opgelost, is alsof access date niet herkent
# => manueel gedaan (csv opgeslagen als xlsx en dan geïmporteerd; csv gaf zelfd eproblemen als access)
        # save_results_access(
        #   results = herb_[1],
        #   database = path_to_analysis_set_db,
        #   remove_tables = TRUE
        # )

# probleem met datumvelden => varTypes aanduiden
con <- odbcConnectAccess2007(path_to_analysis_set_db)

dbtables <- sqlTables(con)
if ("herblayer_by_plot" %in% dbtables$TABLE_NAME) {
        sqlDrop(con, "herblayer_by_plot")
      }
# sqlSave(con, dat = herblayer, tablename = "herblayer_by_plot", fast = FALSE)
sqlSave(con, dat = herb_round[[1]], tablename = "herblayer_by_plot", varTypes = c(date_vegetation="datetime"))

odbcClose(con)

```

Direct naar gdrive exporteren (csv): 
naar **/PRJ_BOSECO_ALGEMEEN/PRJ_BR_AanvraagGegevens/!METADATA-ALL_PLOTS/_plot-level-data/**

```{r save_herb_gdrive, eval = FALSE}
save_results_csv(
    results = herb_plotinfo,
    output_dir = path_to_plotlevel_gdrive
    )

```


**Na update van forresdat, project "forresdat" openen en alles naar de cloud pushen**
**Eventueel ook access op gdrive plaatsen**


# Copy html to teamdrive

Na knitting - manueel

```{r eval = FALSE}
# Specify the path of the file you want to copy
source_file <- here::here(paste0("Scripts/OpbouwDatabank/Main_UpdateForresdat_05_vegetation.html"))

# Specify the destination directory where you want to copy the file
destination_dir <- path_to_html_teamdrive

# Copy the file to the destination directory
file.copy(from = source_file, to = destination_dir, overwrite = TRUE)
# ? file.copy
```
